Javascript:- it is a lightweight, interpreted, high- level programming language.

let :- variable cnnot be re-decleared but can be update. it is a block scope variable.
var:- variable can be re- decleared and update. it is a global scope variable.
const:- var cannot be re- decleared or update. it is a block scope variable.

why we use let instnt of var?
answer:- 
1- var is re- decleared
2- it is a global scope variable
3- var add in window
4- it is function- scoped
5- hoisted and initialzed as undefine
6- temporal dead zone

Hoisting:- it's divide the variable into two parts declaraction and initialization, and declaraction part goes to top and initialization part goes to down.
example:- 
console.log(x);
var x= 12;

DATA TYPE: primitive and non- primitive
primitive:- number,string, bigint, boolean, undefine, null, symbol
non- primitive:- object, array, function

object:-it is a collection of values in key and value format. ex- {name ; "rahul}
array:- it is also a collection of values. ex- [2,3,4,5]

GIT COMMANDS:
git config --global user.name " "
git config --global user.email " "

git remote add origin "Link"

cd .. --this is ued to come out of directory
git init
git add .
git commit -m "message"
git push origin main
git push -f origin main - for forced push


OPERATORS
1- ARITHMETIC OPERATORS:- +,-,*,/, %(Modules),**
2- ASSIGNMENT OPERATORS:- +,+=,-+,*=,%=, **=
3- COMPRASION OPERATORS:- ==,===,!=,!==, >=, <=
4- LOGICAL OPERATORS:- &&(AND),||(OR),!(NOT)


CONDITION STATMENT
1- if 
2- if- else
3- else-if

>- prompt- it is use to take the input from the user.

LOOPS
1-for("condtion"=> start; stop; update){}; ex- for (i=1,1<=10,1++);
2- while("condtion", stop){};
3- do-while loop
4- for- of :- use in strings and array
5- for- in :- use in array and object

Template literals:

`Hello ${a}, ${b} and ${c}!`;

#Arrow Function:
let sum =(x,y)=>x+y;


#Map function:
arr.map(x => x*5);



#Filter function:
filter out the irrelvent values => 
arr.filter(x => typeof(x) === "number");


#Generate Random Value:
Math.random() * 100 //0-100



#Floor:
Math.floor(Math.random()*10+1);


#Ceil:
Math.ceil(Math.random()*100);


#TypeCasting:
let strnum="5";
console.log(Number(strnum));--here string is converted to number
console.log(Integer.parseInt(strnum));//5
console.log(Integer.parseFloat(strnum));//5.0
cosole.log(String(5))//"5"--converts to string


#Array Methods:


1- IndexOf
arr.indexOf("val"); //returns index

2- Includes
arr.includes("val"); //returns boolean value

3- length
arr.length;

4- push
arr.push(val);

5- pop
arr.pop();

6- Unshift
arr.unshift(); //add in the beginning

7- Shift
arr.shift(); //removes from the beginning


# REDUCE FUNCTION 

const GG=[]; // array
const aa= GG.reduce(( empty, array) => {contion}, intial value of new array/object );
EXAMPLE 

const fruitBasket = ['apple', 'banana', 'cherry', 'apple', 'apple', 'banana', 'pineapple']; // apple: 3, banana: 3, cherry: 1, pineapple: 1
const tally = fruitBasket.reduce((acc, fruit) => {
    acc[fruit] = (acc[fruit] || 0) + 1;
    return acc;
}, {}); // intial value of acc is an empty object
console.log(tally);

# DOM Manipulation:

->Login-Form designing
->Navigating to other pages using "window.location.href = "page_name";"
->Manipulating different elements using selectors like id and class;
->Manipulating different elements using selectors like id and class


###  DAY 4 ###  

Asynchronous :- Asynchronous programming in JavaScript allows code to run non-blocking operations, enabling tasks like network requests or file reading without freezing the main thread. 

Promise :- A Promise in JavaScript is an object representing the eventual completion (or failure) of an asynchronous operation and its resulting value.

it has 3 state:
1- pending
2- resolved
3- rejected

example:- 
function getData() {
    return new Promise((resolve, reject) => {
        setTimeout(() => {
            resolve("Data received");
        }, 2000);
    });
}
 
getData().then((data) => {
    console.log(data);
}).catch((error) => {
    console.error("Error:", error);
});

use then ()to handle the fullfilment and catch() to handle rejection.

## setTimeout():- it it use to run a code after the few sec later, mention the time in code.

setTimeout(() => {
     console.log("Data received");
 }, 5000);

## setInterval():- it run the code after every interval of time.
 

 example:=
 setInterval(() => {
    console.log("Checking for new data...");
}, 3000);
 
20. Object Oriented Programming (OOPS) in JavaScript

During the training, I learned how JavaScript supports Object Oriented Programming using classes, objects, and modern ES6 features. The following core OOPS concepts were implemented with examples.

1 Encapsulation

Encapsulation is the process of hiding internal data and allowing access only through public methods.

🔹Example: BankCard with private PIN

class BankCard {
    #pin;

    setPin(pin) {
        if (pin.length === 4 && !isNaN(pin)){
            this.#pin = pin;
        }
    }

    verifyPin(pin) {
        return pin === this.#pin ? "Access Granted" : "Wrong PIN";
    }
}

2 Inheritance

Inheritance allows a child class to reuse properties and methods of a parent class using extends and super.

🔹Example: Vehicle → Car

class Vehicle {
    constructor(brand) {
        this.brand = brand;
    }
    start() {
        console.log("Vehicle started");
    }
}

class Car extends Vehicle {
    constructor(brand, model) {
        super(brand);
        this.model = model;
    }
}

3 Polymorphism

Polymorphism allows the same method name to behave differently depending on the object calling it.

🔹Example: Payment using UPI and Card

class Payment {
    pay() {}
}

class UPI extends Payment {
    pay() {
        console.log("Paid using UPI");
    }
}

class Card extends Payment {
    pay() {
        console.log("Paid using Card");
    }
}

4 Abstraction

Abstraction hides the internal implementation and exposes only essential functionality.

🔹Example: ATM

class ATM {
    #authenticate() {}
    #dispenseCash() {}

    withdraw() {
        this.#authenticate();
        this.#dispenseCash();
    }
}



